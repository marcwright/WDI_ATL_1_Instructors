<%= form_for @event do |f| %>
  <%= f.label :date %>
  <%= f.date_select :date %>

  <%= f.label :time %>
  <%= f.time_select :time %>

  <%= f.label :band_id %>
  <%= f.collection_select(:band_id, Band.all, :id, :name) %>


  <%= f.submit "Submit" %>
<% end %>
