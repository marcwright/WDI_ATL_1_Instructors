<h1><%= @book.title %></h1>

<h2>Characters</h2>
<ul>
  <% @characters.each do |character| %>
    <li><%= link_to character.name, character_path %></li>
  <% end %>
</ul>

<%= link_to "Add a character to '#{@book.title}'", new_book_character_path(@book) %>