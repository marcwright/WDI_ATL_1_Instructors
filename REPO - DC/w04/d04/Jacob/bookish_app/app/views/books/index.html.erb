<h1>Here are the books by <%= @author.name %>.</h1>

<% @books.each do |book| %>
  <ul>
    <li>
      <a href="/authors/<%= book.author_id %>/books/<%= book.id %>"><%= book.title %></a>
    </li>
  </ul>
<% end %>

<a href="/authors/<%= @author.id %>/books/new">Add a book</a>
