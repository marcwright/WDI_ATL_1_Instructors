<h1>This author is <%= @author.name %></h1>

<p>
  <% if @author.gender.downcase == "male" %>He<% else %>She<% end %> was born on <%= @author.date_of_birth %>, and <% if @author.has_pseudonym %>has<% else %>doesn't have<% end %> a pseudonym.
</p>
<a href="/authors/<%= @author.id %>/edit">Edit this author's information</a>
<form action="/authors/<%= @author.id %>" method="post">
  <input type="hidden" name="_method" value="delete">
  <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>" >
  <button>Delete This Author</button>
</form>

