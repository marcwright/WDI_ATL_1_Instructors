//assign numerical values to alphabet
var list = '';
for (var i = 0; i != 26; ++i) list += String.fromCharCode(i + 65);
  return list
// given a 'key word', assign letters sequentially to your desired encoded text

keyWord = "lemon"


// Add corresponding values together

// Use integer to retrieve corresponding letter

