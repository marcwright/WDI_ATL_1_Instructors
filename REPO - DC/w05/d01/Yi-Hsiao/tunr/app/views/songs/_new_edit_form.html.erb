<%= form_for [@artist, @song] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  
  <%= f.label :year %>
  <%= f.text_field :year %>

  <%= f.submit @song.new_record? ? "Add song" : "Update song" %>
<% end %>