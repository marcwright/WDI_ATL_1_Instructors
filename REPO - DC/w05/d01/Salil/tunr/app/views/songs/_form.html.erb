<%= form_for @song, @artist do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :year %>
  <%= f.text_field :year %>

  <%= f.hidden_field :artist_id, :value => @artist.id %>

  <%= f.submit "Submit", id: "submit" %>
<% end %>
