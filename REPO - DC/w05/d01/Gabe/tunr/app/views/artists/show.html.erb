<%= @artist.name %>
<%= @artist.genre %>
<%= image_tag @artist.photo_url, alt: "", class: "artist" %>


Songs:

add a song:
<%= link_to new_artist_song_path, song %>

<% @songs.each do |song| %>
  <li>
    song title:
    <%= song.title %>

    update this song:
    <%= link_to song.title, song %>

    delete this song:
    <%= form_for @artist, method: 'delete' do |f| %>
  <%= f.submit 'destroy' %>
<% end %>



</li><% end %>


delete this artist:<br>
<%= form_for @artist, method: 'delete' do |f| %>
  <%= f.submit 'destroy' %>
<% end %>
