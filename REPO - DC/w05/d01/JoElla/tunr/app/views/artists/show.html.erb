<h1>Artist Info</h1>

<%= @artist.name %>
<%= @artist.genre %>
<%= @artist.photo_url %>



<%= form_for @artist, method: 'delete' do |f| %>
  <%= f.submit "destroy" %>
<% end %>

<% @songs.each do |song| %>
  <%= song.title %>
  <%= song.year %>
  <% end %>
