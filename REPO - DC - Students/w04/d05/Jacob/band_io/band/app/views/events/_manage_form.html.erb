<%= form_for @event do |f| %>
  <%= f.label :date %>
  <%= date_select :date %>

  <%= f.label :time %>
  <%= f.time :time %>

  <%= options_from_collection_for_select(Band.all, :id, :name) %>

  <%= f.submit "Submit" %>
<% end %>
