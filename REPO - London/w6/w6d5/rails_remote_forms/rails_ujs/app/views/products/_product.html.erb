<article>
  <h1><%= link_to product.name, product %> <%= number_to_currency product.price %></h1>
  <p><%= link_to 'Edit', edit_product_path(product) %> | <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>
 </article>
