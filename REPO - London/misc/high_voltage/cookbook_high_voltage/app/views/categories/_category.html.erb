<div class="container2">
  <h2><%=link_to category.name, category_path(category.id)%></h2>
  <%= link_to 'delete', category_path(category.id), method: :delete, confirm: 'Are you sure?'%>
  <%= link_to 'edit', edit_category_path(category.id)%>
</div>