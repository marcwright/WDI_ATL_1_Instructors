<div class="container2">
  <h2><%=link_to ingredient.name, ingredient_path(ingredient.id)%></h2>
  <%= link_to 'delete', ingredient_path(ingredient.id), method: :delete, confirm: 'Are you sure?'%>
  <%= link_to 'edit', edit_ingredient_path(ingredient.id)%>
</div>